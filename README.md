# tendar-api-project
Python/Flask API for the Tendar application. This repository contains the production Dockerfile and the Kubernetes manifest (deployment.yaml) used for automated deployment via Argo CD to the EKS Fargate staging environment.
